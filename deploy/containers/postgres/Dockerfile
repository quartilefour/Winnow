FROM postgres

ARG ENV_POSTGRES_PASSWORD=test
ENV POSTGRES_PASSWORD=$ENV_POSTGRES_PASSWORD

ARG ENV_POSTGRES_USER=admin
ENV POSTGRES_USER=$ENV_POSTGRES_USER

ARG ENV_POSTGRES_DB=gfn 
ENV POSTGRES_DB=$ENV_POSTGRES_DB

COPY ./sql/V1__initialize.sql /docker-entrypoint-initdb.d/

EXPOSE 5432

